# OpenAI-Compatible API Configuration
# Copy this file to .env and fill in your actual values

# Base URL for the OpenAI-compatible API
# Example: https://api.minimax.chat (for MiniMax API)
OPENAI_BASE_URL=https://api.unieai.com

# Your API key for authentication
OPENAI_API_KEY=your_api_key_here

# Default model to use (MiniMax-M2 is set as default)
OPENAI_MODEL=MiniMax-M2

# Optional: Custom system prompt
# SYSTEM_PROMPT=You are a helpful voice assistant. Keep responses concise and conversational.

# Optional: Maximum conversation history to send to API
# MAX_HISTORY=10

# Public model name for client-side display (must have NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_OPENAI_MODEL=MiniMax-M2
