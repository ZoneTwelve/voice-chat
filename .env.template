# OpenAI-Compatible API Configuration Template
# Copy this file to .env.local and replace the placeholder values with your actual credentials

# Base URL for the OpenAI-compatible API endpoint
# For MiniMax API, use: https://api.minimax.chat
# For other providers, use their respective base URL
OPENAI_BASE_URL=https://api.minimax.chat

# Your API key for authentication
# Replace 'your_api_key_here' with your actual API key
OPENAI_API_KEY=your_api_key_here

# Default model to use for chat completions
# Examples: MiniMax-M2, gpt-3.5-turbo, gpt-4, etc.
OPENAI_MODEL=MiniMax-M2

# Optional: Custom system prompt (uncomment and modify if needed)
# SYSTEM_PROMPT=You are a helpful voice assistant. Keep responses concise and conversational.

# Optional: Maximum conversation history to send to API (default: 10)
# MAX_HISTORY=10

# Optional: Enable debug logging (uncomment to enable)
# DEBUG=true
